<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.daddysoffice.sample.broadcasttest"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk tools:overrideLibrary="android.support.v4,android.support.mediacompat,     android.support.fragment,android.support.coreui,android.support.coreutils,     android.support.v7.appcompat,android.support.graphics.drawable,android.support.loader,     android.support.v7.viewpager,android.support.coordinatorlayout,android.support.drawerlayout,     android.support.slidingpanelayout,android.support.customview,android.support.swiperefreshlayout,     android.support.asynclayoutinflater,android.support.compat,androidx.versionedparcelable,     android.arch.lifecycle,android.support.documentfile,android.support.localbroadcastmanager,     android.support.print,android.support.interpolator,     android.support.cursoradapter,android.arch.lifecycle.viewmodel,android.arch.lifecycle.livedata,     android.arch.lifecycle.livedata.core,android.arch.core" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">

        <activity android:name=".PermissionConfirmActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="動画配信テスト"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"/>

    </application>

</manifest>
